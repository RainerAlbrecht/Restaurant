{% extends 'base.html.twig' %}

{% block title %}Hello GerichtController!{% endblock %}

{% block body %}
<br>
<div class="container">
  <h1>  {{gericht.name}}</h1>
  <img class="img-thumbnail" src="/bilder/{{gericht.bild}}" alt="{{gericht.name}}">
</div>
{% endblock %}
